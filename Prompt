You are a senior Product Designer, UX Architect and Software Architect.

Your mission is NOT to write code.

Your mission is to produce a complete UI/UX specification document for an existing desktop application before any redesign begins.

The application already works.

DO NOT redesign the data model.

DO NOT redesign the business logic.

DO NOT modify workflows.

DO NOT remove any existing feature.

The redesign is visual and architectural only.

The final document must become the reference specification for all future development.

Everything must be written in English.

The document must be structured, professional and exhaustive, as if it were written by a lead UX designer for a software company.

The application is an Electron desktop application built with:

- React 18
- TypeScript
- Vite
- Electron
- Zustand
- Dexie
- Tailwind
- Recharts
- Zod

Desktop only.

No responsive/mobile version.

The application is an engineering tool used to browse military environmental standards (MIL-STD, STANAG, etc.) without reading thousands of PDF pages.

The application is NOT a database editor.

The application is NOT a Git client.

The application is first and foremost a STANDARD BROWSER.

The Browser page is the core of the entire application.

Every other page must reuse the same design language and visual components.

The application philosophy is:

- laboratory software
- engineering software
- professional tool
- technical
- clean
- minimal
- IDE inspired

The visual references are:

- Visual Studio Code
- JetBrains IDE
- GitHub Desktop
- Linear
- Figma

Avoid:

- Material Design feeling
- rounded playful UI
- unnecessary colors
- shadows everywhere

The design language must remain neutral and technical.

The only semantic colors are:

Green = Official

Orange = Pending admin review

Yellow = Local changes

Blue = Current selection

Red = Error

Grey = Navigation

Everything else must remain monochrome.

The Browser page must contain:

- application logo (top left)
- application name
- global search bar
- Management button (top right, hidden for read-only users)

Main layout:

Left:
Standard selector.

Then a true Miller Columns browser exactly like macOS Finder.

The Miller browser expands horizontally as deep as necessary.

Right:
Information panel.

If a taxonomy node is selected:
display node information.

If a profile is selected:
display the profile card.

The Miller browser and the information panel are always attached.

A draggable separator allows resizing both.

Neither panel may disappear completely.

The separator must always remain accessible.

Profile cards can be pinned.

Pinned profiles open as additional columns to the right.

Each pinned profile owns its own independent Miller browser.

Panels behave like VSCode:

- dockable
- closable
- horizontally resizable

The Browser defines the visual language of the entire application.

Management is the second top-level page.

Management contains:

- Home
- Edit database
- Synchronization
- Settings
- Admin (admin only)

Edit Database contains a Miller browser identical to Browser.

Selecting a taxonomy node displays profile editing.

A gear icon switches to Taxonomy editing mode.

Taxonomy editing reuses the Miller browser but becomes editable.

Nodes may be added, removed or modified.

Profile cards must use exactly the same component everywhere:

- Browser
- Edition
- Creation
- Review
- Comparison
- Admin

The displayed fields are completely dynamic.

The application never assumes which fields exist.

Cards adapt automatically to the selected standard.

The internal layout remains visually identical regardless of content.

Charts remain integrated inside profile cards.

Charts can be expanded.

Synchronization page:

Shows every local modification.

Grouped by:

- Standards
- Taxonomy
- Profiles

Filtering:

- standard
- date
- modification type
- review status

Selecting a modification opens a comparison panel.

Comparison must reuse the same profile card layout.

Modified fields are highlighted.

Admin page:

Tab 1:

Review user submissions.

Resolve conflicts.

Approve or reject.

Tab 2:

Git history visualization.

Visual commit tree inspired by GitHub.

Tab 3:

Connected users.

Permissions:

Read Only

Write

Admin

Settings page:

Import database JSON

Export merged database JSON

Export online database

Git repository location

Session name

Application version

Credits

Global UX rules:

Every page must use:

the same toolbar

the same spacing

the same typography

the same Miller component

the same Profile Card component

the same color system

the same icons

the same resize handles

the same contextual menus

The redesign must NEVER modify business logic.

It must NEVER change IndexedDB.

It must NEVER modify synchronization logic.

It must NEVER modify Git logic.

It must NEVER modify Electron logic.

It must NEVER change existing features.

Deliver a complete UI/UX Specification including:

1. Product philosophy

2. User personas

3. Information architecture

4. Navigation

5. Complete screen hierarchy

6. Design System

7. Typography

8. Colors

9. Spacing system

10. Component library

11. Docking system

12. Miller Browser specification

13. Profile Card specification

14. Browser page

15. Management pages

16. Synchronization page

17. Admin pages

18. Settings page

19. Icons

20. Animations

21. Interaction rules

22. Accessibility

23. Empty states

24. Loading states

25. Error states

26. Future extensibility

27. Non-regression constraints

Whenever useful, include wireframes using ASCII diagrams.

Whenever useful, create UI mockups.

Whenever useful, explain why a design choice improves engineering workflows.

The document must be detailed enough that another AI can redesign the application without inventing anything.
